{
  "name": "localfr-resultats-functions",
  "description": "Firebase Cloud Functions — local.fr Résultats Journaliers",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^7.2.5"
  },
  "private": true
}
